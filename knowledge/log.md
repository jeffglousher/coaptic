# Directory Update Log

Current on this tree (main through #34 plus this change): message codec, bounded storage, `Endpoint` (not `Peer`), `Access` pins, `Ids` / Token mint, CON RTO on `PendingCon`, Exchange / Dedup / Observe tables, Observe Max-Age / client-OFF lifetime and RFC 7641 §4.5 / §4.5.1 24-hour NON-confirm + notification NSTART on `ObserveInterest`, classic Block + Q-Block send/receive, BERT (SZX 7) multi-block payloads, Request-Tag / ETag [`BodyTag`](../src/storage/block.rs) on [`BlockKey`](../src/storage/block.rs), Echo on [`ExchangeEntry`](../src/storage/exchange.rs), first-block Observe on outgoing Block2 (`encode_block2_observe_tx`), FETCH / PATCH / iPATCH codes, Hop-Limit, No-Response, If-Match / If-None-Match [`Precondition`](../src/message/precondition.rs), `Engine::progress` (retransmit, RX, Observe notify, Observe lifetime, `QBlockRecover`). Caller contract: [`CALLER.md`](../CALLER.md). 0.1 API freeze: [`REVIEW.md`](../REVIEW.md). [`design.md`](../design.md) Validation and tuning matches that surface. In-crate harness: `cargo test --test block_sweep` and `cargo test --test plugtest`. DTLS / OSCORE deferred. 6LoWPAN not planned. No crates.io publish.

Older bullets are PR-time notes. Present-tense "not implemented" or "stays a stub" in those notes is historical scope for that change, not current tree state.

## 2026-09-05

* **Update**: Caller-must checklist ([`CALLER.md`](../CALLER.md): send, clock, jitter, RST/4.xx, resource/If-Match, Encode+Access). Recorded 0.1 API surface freeze in [`REVIEW.md`](../REVIEW.md) (crate-root happy path vs `storage::` / `message::`; no drive-by renames). Confirmed 6LoWPAN skip reason is **not planned**. No protocol feature. No `design.md` change.
* **Update**: Marked 6LoWPAN / `TD_6LoWPAN_*` **not planned** (skip reason, catalog inventory, REVIEW / README / plugtest knowledge / skill / SECURITY). DTLS / OSCORE stay deferred. Core CoAP bar note in [`REVIEW.md`](../REVIEW.md). Message-layer gaps: FETCH / PATCH / iPATCH plus 4.09 / 4.22 / 5.08 named codes; [`HopLimit`](../src/message/hop.rs) (RFC 8768); [`NoResponse`](../src/message/no_response.rs) (RFC 7967); If-Match / If-None-Match [`Precondition`](../src/message/precondition.rs); Observe register/deregister on FETCH. No send / 4.xx / 5.08 / forwarding policy. No seventh area. No `design.md` change.
* **Update**: RFC 7641 §4.5 / §4.5.1 notification transmission on the existing [`ObserveInterest`](../src/storage/table.rs) row ([`ObserveNotifyHold`](../src/storage/table.rs), [`ObserveTransmission`](../src/message/mod.rs)). [`Engine::record_observe_notify`] records CON (resets the 24-hour confirm clock; NSTART hold until [`Engine::ack_observe_con`]) or NON (starts the 24-hour clock; hold for 3 s). [`ObserveInterest::must_confirm`] is the 24-hour NON-confirm MUST. [`Engine::progress`] skips a pending notify when that endpoint is already at notification NSTART. [`Engine::encode_block2_observe_tx`] writes Observe on the first outgoing Block2 of a notification. Named [`Code::CONTINUE`] (2.31) and [`Code::REQUEST_ENTITY_INCOMPLETE`] (4.08) — no send policy. No seventh area. No `design.md` change.
* **Update**: Echo option (RFC 9175) at the library + exchange layer. [`Echo`](../src/message/echo.rs) is opaque 1..=40; [`Opt::echo`] / [`ParsedMessage::echo`] encode/decode. Time-based [`Echo::mint`] packs caller `now_ms` plus caller pad (no MAC crate, no OS RNG); [`EchoFreshness`] / [`Engine::echo_freshness`] classify missing / invalid / stale / fresh. Event-based freshness is equality. [`ExchangeEntry`](../src/storage/exchange.rs) stores the Echo sent on the request and a response [`ExchangeEntry::challenge`] (bound to the remote [`Endpoint`]). [`Engine::record_request`] / [`Engine::match_response`] capture those. No seventh area, no 4.01 / RST / 4.02 policy. Did not rewrite `design.md` ownership; #31 already synced Validation and tuning.
* **Update**: Synced [`design.md`](../design.md) Validation and tuning to the current crate: `Access` pins, `Engine::progress`, Observe notify + Max-Age / client-OFF lifetime, Q-Block recover, BERT + `BodyTag` identity, in-crate harness. [`REVIEW.md`](../REVIEW.md) no longer treats those as deferred. Ownership, six areas, datagram = CoAP payload, `Endpoint` sidecar, and block-wise builder rules unchanged. No protocol feature code.
* **Update**: BERT (SZX 7) and Request-Tag / ETag body identity on the existing Body Slot / [`BlockTransfer`](../src/storage/block.rs) sidecar ([`BodyTag`](../src/storage/block.rs) on [`BlockKey`](../src/storage/block.rs)). Incoming classic Block partitions a multi-block BERT payload at NUM×1024; outgoing `issue_bert_outgoing` / `next_bert1` / `next_bert2` cap one datagram. Apply matches a present tag across Tokens at the same endpoint; a different tag is a different body. `apply_*_rx` and encode helpers read/write the first Request-Tag (Block1 / Q-Block1) or ETag (Block2 / Q-Block2). Q-Block rejects SZX 7. No seventh area, no new deps, no RST / 4.02 / 2.31 / 4.08 policy. No `design.md` change.
* **Update**: Observe Max-Age / client-OFF lifetime on the existing [`ObserveInterest`](../src/storage/table.rs) row (`ObserveLifetime`: freshness or unacked CON; absolute `due_ms` from caller `now_ms`). [`Engine::refresh_observe_max_age`] sets/refreshes; [`Engine::ack_observe_con`] clears CON-wait after empty ACK; [`Engine::progress`] / [`Engine::poll_observe_lifetime`] surfaces at most one [`ObserveExpiry`] (row stays occupied). CON `Retransmit::GiveUp` marks a matching unacked notify due. No seventh area, no RST / 4.02 policy. Unlocks `TD_COAP_OBS_04` / `TD_COAP_OBS_05`. No `design.md` change.
* **Update**: Validation harness. `tests/block_sweep.rs` is the combinatorial SZX `{16…1024}` × body length in blocks `1…25` sweep (classic Block1/Block2 assembly and Q-Block windowed send/receive) on a large `SweepProfile` (32 × 1024; `AllocMemory` path under `alloc`). `tests/plugtest/` drives in-scope CoAP#4 TDs from vendored YAML on a two-`Engine` loopback (no sockets, no DTLS). Hand-maintained TD id lists are checked against `knowledge/plugtest/td-coap4/*.yml` keys. `dtls` / `6lowpan` skipped. Run: `cargo test --test block_sweep` and `cargo test --test plugtest`. No new Cargo deps. No `design.md` change.
* **Update**: Review-readiness docs pass. Scannable [README](../README.md) / crate rustdoc; [`REVIEW.md`](../REVIEW.md) for an external reviewer; knowledge names synced (`Endpoint`, `Access`, `Progress`, `QBlockRecover`); log preamble so older "not implemented" bullets read as PR-time scope. No protocol features. This change did not land an in-crate harness. No `design.md` change.
* **Update**: Core hot-path / API-surface cleanup. [`Token::from_checked`](../src/message/mod.rs) is the infallible decode construction after TKL is proven (no `expect` on the parse path). Crate-root re-exports are the happy path (`Engine` / `Memory` / decode-encode / `Endpoint` / `Progress` / `Access` / `Ids` / keyed rows / Block types / main errors). Typestate `Missing` / `Present`, raw `*Table` / `*Pool` types, and backend traits stay in `storage` / `message`. Dual `Engine::acquire_dedup` / `acquire_observe` (+ release/rotate_dedup) are removed; public callers use typed `insert_*` / `take_*`. `rotate_observe` stays `pub(crate)` for progress fairness. Thin `#[inline]` on Engine BodySlots forwards. Builder typestate `expect` after proven gates remains. No behavior change, no new deps, no plugtest harness, no `design.md` change.
* **Update**: Q-Block missing-block recovery on the existing Incoming Body Pool / [`BlockTransfer`](../src/storage/block.rs) window bitmap. [`Engine::progress`] surfaces at most one [`QBlockRecover`](../src/storage/block.rs) per pass (rotating RX body cursor): first known hole below the highest received NUM, or at/before M=0, plus a compact in-window hole mask. Incoming Q-Block2 recover encodes repeatable Q-Block2 options (`encode_q_block2_recover_tx`). Incoming Q-Block1 stays structured work (caller owns 4.08). Outgoing reissue (`reissue_q_block1` / `reissue_q_block2`) reads the complete body without changing window state. `.block_wise(false)` stays `None` / `NoBodyPools`. No 4.08 / 2.31 / RST policy. No BERT, SZX×1–25 harness, or `design.md` change.
* **Update**: Observe notify on the existing [`ObserveInterest`](../src/storage/table.rs) row (`seq` + `pending`; no seventh area, no queued bodies). [`Engine::signal_observe`] marks due (coalesces). [`Engine::progress`] surfaces at most one pending interest per pass from the Observe cursor, assigns the next 24-bit sequence (RFC 7641 wrap), and clears pending. Caller encodes the notification with existing Observe option helpers and Access. Q-Block missing-block recovery was still deferred in this change. No `design.md` change.
* **Update**: Bounded [`Engine::progress`](../src/storage/progress.rs) (`Progress`: at most one `poll_retransmit` plus one rotating unpinned RX datagram step per call). Caller `now_ms`; no OS clock; no send; no alloc; no storage growth. Access pins are skipped (progress does not release pinned slots). Observe notify and Q-Block missing-block recovery were still documented stubs (`None`) in this change. No `design.md` change.
* **Update**: Temporary application [`Access`](../src/storage/access.rs) / `AccessMut` to occupied RX/TX datagram and body bytes. Pin bit on the occupancy word: `release` of that id is [`SlotError::Pinned`] until `Drop` unpins. Rotate is cursor-only and does not evict, so it is not blocked. Exclusive: a second pin of the same slot is `Pinned`. No alloc, no unsafe. Engine: `access_rx` / `access_tx` / `access_tx_mut` / `access_rx_body` / `access_tx_body_mut`. Not progress loop, Observe notify, or Q-Block missing-block recovery. No `design.md` change.
* **Update**: Message ID sequence (`Ids`: wrapping `u16` counter) and Token mint (`Token::mint` / `Token::mint_from` + `TokenSource`). Caller-owned, `Copy` / `no_std`. Core does not call an OS RNG (same posture as `Transmission` jitter). CON/NON request skeletons (`Message::con` / `Message::non`, `Ids::request`) take the next MID + token with no `Engine`. Token length remains 0..=8. This change did not crate-root re-export `Ids` / `TokenSource`. Access, progress, Observe notify, and Q-Block recover were still deferred in this change. No `design.md` change.

## 2026-09-04

* **Update**: CON retransmission / RTO on the existing `PendingCon` TX sidecar (`PendingRto`: attempts, next-due, current timeout). Caller injects `now_ms` and `jitter_ms` (core has no OS clock or RNG; `Transmission::initial_timeout_ms` clamps jitter to the ACK_RANDOM_FACTOR span). `poll_retransmit` scans pending TX slots (O(n)); exponential backoff; `Retransmit::GiveUp` at `MAX_RETRANSMIT`. Empty ACK/RST already cleared pending and now clears RTO. `NSTART` is a per-endpoint outstanding-CON count on `record_pending_con` (message layer only; not exchange/NON). No socket send. No `design.md` change.
* **Update**: Outgoing Q-Block1 / Q-Block2 windowed send on the existing Outgoing Body Pool (`BlockTransfer` issued-window bitmap, RFC 9177 §7.2 `MAX_PAYLOADS` default 10). Issue up to 10 outstanding NUMs per window (increasing NUM). Window advances on a peer Continue NUM (Q-Block1: last of `MAX_PAYLOADS_SET`; Q-Block2: next-window base). Empty ACK is not a window ACK. Encode helpers write Q-Block + Size1/Size2. Classic Block and incoming Q-Block paths unchanged. This change did not add BERT, missing-block recovery, Request-Tag/ETag identity, or an SZX×1–25 harness. No `design.md` change.
* **Update**: Incoming Q-Block1 / Q-Block2 multi-window receive on the existing body pools (`BlockTransfer` window bitmap, RFC 9177 §7.2 `MAX_PAYLOADS` default 10). Out-of-order in-window fill, duplicate / outside-window reject, window advance, complete on M=0 with gaps filled. Classic Block paths unchanged. No BERT, Q-Block transmit, Request-Tag/ETag identity, or SZX×1–25 harness. No `design.md` change.
* **Update**: Classic Block client directions on the same `BlockTransfer` / `BodySlots` model: incoming Block2 (admit/write/apply/`apply_block2_rx`) and outgoing Block1 (start/`next_block1`/`encode_block1_tx`). Same in-order / SZX-lock / overflow rules as incoming Block1 and outgoing Block2. No Q-Block window, BERT, or SZX×1–25 harness. No `design.md` change.
* **Update**: Classic Block1 / Block2 body assembly into existing Incoming / Outgoing Body Pool slots (`BlockTransfer` sidecar: Token + remote Endpoint, NUM/SZX/More, next NUM, bytes filled). Incoming Block1 admit/write/complete/release and outgoing Block2 start/next/encode. No Q-Block window, BERT, or SZX×1–25 harness. No `design.md` change.
* **Update**: Block-wise option codecs at the library layer: Block1 (27) / Block2 (23) / Size2 (28) from RFC 7959 and Q-Block1 (19) / Q-Block2 (31) from RFC 9177. `BlockValue` is NUM/M/SZX encode/decode (Q-Block shares the bitfields). Not in RFC 7252 Table 4; `is_rfc7252` unchanged. No body reassembly. Enabled body slots remain a multiple of 1024 (max SZX). No `design.md` change.
* **Update**: RFC 7641 Observe at the library + table layer: option 6 uint codec (`encode_observe` / `decode_observe`, register 0 / deregister 1 / 24-bit sequence), typed `ObserveKey` / `ObserveInterest` rows (Token + remote Endpoint) in the existing `ObserveTable`, and Engine register/deregister glue. Not in RFC 7252 Table 4; elective. No notification scheduler. No `design.md` change.
* **Update**: Token-based request/response matching (`ExchangeKey` / `ExchangeEntry` / `ExchangeTable`: Token + remote Endpoint). Compact table sized from the TX Datagram Pool count, independent occupancy so separate/NON responses outlive the TX slot. Dedup and pending CON remain separate identities. Empty ACK (0.00) does not complete an exchange; piggybacked ACK with a response code does. No seventh area; no `design.md` change.
* **Update**: Empty ACK/RST constructors plus pending-CON matching as TX datagram sidecar (`PendingCon`: Message ID + remote Endpoint + TX `SlotId`). Dedup remains a separate identity. No seventh area; no `design.md` change. Retransmit / RTO / NSTART algorithms were still deferred in this change (`Transmission` named constants only).
* **Update**: Replaced the `Peer` placeholder with [`Endpoint`](../src/storage/endpoint.rs) (IPv4/IPv6 sidecar) and typed Dedup Table rows (`DedupKey` / `DedupEntry`: Message ID + remote Endpoint). No `design.md` change.
* **Update**: Fixed-capacity `OptionsBuilder` for non-decreasing option encode; thin `Engine` ↔ message slot glue (`decode_rx` / `encode_tx` and mirrors). No `design.md` change.
* **Update**: RFC 7252 §3.2 option value codecs on the library façade (`message::value`). Optional format check is separate from wire decode and from unrecognized-critical. No `design.md` change.
* **Update**: Locked block-wise as an explicit builder switch: disabled ⇒ no body pools; enabled body bytes are a multiple of 1024 (default **4096** = 4 × 1024). Added [Block testing](/block-testing.md) combinatorial SZX × 1–25-block sweep (spec only).

## 2026-09-03

* **Update**: Added locked crate memory/API concepts [Memory](/memory.md) and [Profiles](/profiles.md) (1472 datagram default, Storage trait, named `MemoryProfile` constants). Spec only; no engine implementation.
* **Update**: Added the repo-specific `okf-frontmatter` skill (extract → enrich → validate, fail closed, including PR CI job `okf`) and regenerated concept frontmatter from a fresh deterministic extract plus LLM enrich/dedupe.
* **Update**: Moved IETF RFC copies into [RFCs](/rfcs/index.md) (one concept per RFC plus txt/pdf). Added [Plugtest](/plugtest/index.md) and [plugtest requirements](/plugtest/requirements.md). Replaced generic skills with `bounded-engine` and `plugtest`.
* **Creation**: Established the OKF v0.2 bundle and concepts [coaptic](/crate.md), [Architecture](/architecture.md), [Memory areas](/memory-areas.md), [CI](/ci.md), and [Review](/review.md).
